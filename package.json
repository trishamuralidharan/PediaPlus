{
  "name": "pediaplus",
  "version": "2.0.0",
  "description": "PediaPlus – Smart Child Health Companion",
  "scripts": {
    "build:apk":   "npx cap add android && npx cap sync android && npx cap build android",
    "open:android":"npx cap open android",
    "sync":        "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core":    "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar":    "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
